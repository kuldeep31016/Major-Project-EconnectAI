# Synthetic test geometry

Deterministic prototype patch geometry used ONLY by the test-suite and by
`scripts/run_graph_analysis.py --source prototype` to exercise the graph mathematics without a
segmentation run. Every run produced from it is labelled `PROTOTYPE / SYNTHETIC` and is never shown
in the platform as a result. Site names/extents are real; patches, areas and confidences are not.
