# data/

Datasets are **not** committed. Set `DATA_ROOT` in `.env` (or `configs/dataset.yaml`) to wherever the
dataset lives — local disk, external drive, or a mounted Google Drive in Colab.

See `docs/DATASET_SETUP.md` for the expected layout and the acquisition scripts.
